#include<stdio.h>
#include<stdlib.h>
#include<string.h>
//字符串长度
int main()
{
	char arr[] = "hello world";
	
	//strlen :获取字符串的有效长度(忽略'\0' 不忽略\n) 只能处理字符串，不能处理字符数组
	/*int len = strlen(arr);

	printf("字符串的长度:%d\n",sizeof(arr));
	printf("字符串的有效长度:%d",len);*/





	return 0;
}